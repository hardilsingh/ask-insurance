/**
 * True while the Support (chat) tab is focused. Used to avoid duplicate
 * banners/sounds for chat pushes when the user is already on that screen.
 */
export const supportChatFocusedRef = { current: false };
